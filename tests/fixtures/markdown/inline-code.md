# Inline Code

Use the `console.log()` function to debug.

The `parseFile` and `serializeAST` functions are key.

Backtick in code: `` `escaped` `` using double backticks.

Multiple: `foo`, `bar`, and `baz`.
